import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false,
  output: "export",
  basePath: "/REST-Countries-API-with-color-theme-switcher-with-Nextj",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
